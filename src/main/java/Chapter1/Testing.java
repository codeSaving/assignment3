package Chapter1;

public class Testing {
    public static void main(String[] args) {
             // we initilize x to 0
        int x = 0;
        // we excute the loop until x reaches 10
        while (x < 10) {
            // we update x by incrementing it by 2
            x = x + 2;
            /*
            if x is 0 --> 0+ 2 = 2
            if x is 2 --> 2 + = 4 [...]
            if x is 8 --> 8+2 = 10

             */

            // display the value of the loop
            System.out.println("x is " + x);
        }
        // here is the output:








    }
}
